const randCoord = () => Math.random()

class HasCoordinate {
    constructor (x, y, speedX, speedY) {
        this.x = x
        this.y = y
        this.speedX = speedX
        this.speedY = speedY
    }
}